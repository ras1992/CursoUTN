//registro de datos usuario
git config --global user.name “firstname lastname”
git config --global user.email “valid-email”

//actualizar archivos a la nube
git status
git add .
git commit -m "Ejercitación js"
git push
